# Local secrets/config — copy .env.example instead
.env

# UI-saved settings (scope/org/repo/label) — see src/Settings.php
/storage/

# Custom branding — drop your own logo.png here, it's optional at runtime
public/assets/logo.png

# Editor/OS cruft
.DS_Store
*.swp
.idea/
.vscode/

# Dev tooling (composer install regenerates this; see README)
/vendor/
.phpunit.result.cache
.phpunit.cache/

# Dev tooling (npm install regenerates this; see README)
/node_modules/
/playwright-report/
/test-results/
package-lock.json
